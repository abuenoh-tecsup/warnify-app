<input
    type="{{ $type ?? 'text' }}"
    name="{{ $name ?? '' }}"
    id="{{ $name ?? '' }}"
    placeholder="{{ $placeholder ?? '' }}"
    class="w-full px-4 py-2 mt-1 rounded-xl"
    value="{{ $value ?? '' }}"
/>
